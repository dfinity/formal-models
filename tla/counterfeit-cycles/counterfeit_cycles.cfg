CONSTANTS
    SUBNETS = {"s1", "s2"}
    STARTING_BALANCE_PER_SUBNET = 2
    MAX_DISHONEST_TRANSFERS = 3
    MAX_TRANSFERS = 5

INIT Init
NEXT Next

INVARIANT
NoFakeCyclesAreCreated
LedgerAndSubnetBalancesMatchAfterMsgProcessing
SubnetNeverHasMoreBalanceThanLedger

\* Because the number of transfers are bounded by MAX_TRANSFERS,
\* a deadlock is reached when the number of transfers reaches MAX_TRANSFERS.
CHECK_DEADLOCK FALSE
